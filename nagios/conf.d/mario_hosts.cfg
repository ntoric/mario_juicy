define host {
    use                     linux-server
    host_name               pos_db
    alias                   PostgreSQL Database
    address                 db
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               pos_redis
    alias                   Redis Cache
    address                 redis
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               pos_backend
    alias                   Django Backend
    address                 web
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               pos_go_backend
    alias                   Go Backend
    address                 go_backend
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               pos_frontend
    alias                   Next.js Frontend
    address                 frontend
    check_command           check-host-alive
}
